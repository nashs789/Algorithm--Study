class Solution {
    public int solution(int n) {
        int answer = 0;

        for(int idx = 1; idx < n; idx++){
            if(n % idx == 1){
                answer = idx;
                break;
            }
        }

        return answer;
    }
}